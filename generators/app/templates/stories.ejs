import React from 'react';
import { storiesOf } from '@storybook/react';
// import { whiteBackground } from '/.storybook/decorators';
import Component from './component';
import cases from './cases.spec.js';

const stories = storiesOf('<%= startName %>', module)
//  .addDecorator(whiteBackground);

for (const [caseName, caseData] of cases) {
  stories.addCentered(caseName, () => (
    <Component {...caseData} />
  ));
}
