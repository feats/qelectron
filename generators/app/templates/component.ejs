// import _ from 'lodash';
import React, { PropTypes } from 'react';<% if(style){ %>
import styled from 'styled-components';
import { Wrapper } from './elements';<% } %><% if(style){ %>

const Title = styled.div`
  color: palevioletred;
  font-size: 1.5em;
  text-align: center;
`;<% } %>

const Component = ({ children, ...props }) => (<% if(style){ %>
  <Wrapper {...props}>
    <Title>
      Hello World!
    </Title>
  </Wrapper><% } %><% if(!style){ %>
  <div {...props}>
    Hello World!
  </div><% } %>
);

Component.propTypes = {
  // onChange: PropTypes.func.isRequired,
};

Component.displayName = '<%= camelName %>Component';
export default Component;
