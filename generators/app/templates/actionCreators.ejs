// import _ from 'lodash';
import { createAction } from 'redux-actions';
import { COMPONENT_KEY } from './constants';

export const mySweetAction = createAction(`${COMPONENT_KEY}/DO_WHATEVER`, async () => {
  return await doWhatever();
});
