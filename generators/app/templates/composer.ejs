import _ from 'lodash';
import { merge, compose } from '/imports/client/compose';
// import { NotAuthorizedError } from '/imports/errors';
// import { restrictAccess } from '/imports/client/composers';
// import { COMPONENT_KEY } from '../constants';

const composer = (props, onData) => {
//   if (!somethingHappens) {
//     return onData(new NotAuthorizedError('SendWhateverErrorMessage'));
//   }

  return onData(null, {
    ...props,
  });
};

// const stateMapper = (props, { ...reduxState }) => ({
//   ...reduxState,
//   ...props,
// });


export default merge(
  compose(composer),
  // restrictAccess,
);
